# Конфигурация MongoDB
spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/newsdb
      database: newsdb
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true