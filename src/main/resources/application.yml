spring:
  application:
    name: Module 1
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
contact:
  file:
    path: src/main/resources/contacts.txt